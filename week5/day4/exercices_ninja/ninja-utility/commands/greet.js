import chalk from "chalk";

function greet() {
  console.log(chalk.green(chalk.bold("Welcome, Ninja! ⚔️")));
  console.log(chalk.blue("Prepare yourself for epic CLI commands!"));
}

export default greet;


